{
  "name": "tssSaver",
  "version": "0.1.1",
  "description": "tssChecker GUI wrapper.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^10.1.2"
  },
  "dependencies": {
    "fs-extra": "^9.0.1"
  }
}
